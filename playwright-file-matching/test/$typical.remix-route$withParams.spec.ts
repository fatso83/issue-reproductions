import { expect, test } from "@playwright/test";

import {fail} from "assert";

test.describe("Path Loader", () => {
  test("should return a response", async () => {
      fail("I will never ever run")
  });
});

